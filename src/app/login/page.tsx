import AuthCard from '@/components/AuthCard';

export default function LoginPage() {
  return (
    <main className="w-full min-h-screen flex items-center justify-center bg-[#FFFFFF] selection:bg-[#111] selection:text-white p-6">
      <AuthCard />
    </main>
  );
}
